const config = {
  tabWidth: 2,
  useTabs: false,
  singleQuote: true,
  arrowParens: 'always',
  printWidth: 100,
  proseWrap: 'always',
  bracketSpacing: true,
  trailingComma: 'es5',
  semi: true,
  plugins: ['prettier-plugin-tailwindcss'],
};

export default config;
